<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.isi.Mapper.UserMapper">
<select id="SelectUserId" parameterType="com.isi.pojo.User" resultType="Integer">
    select user_id from user where institution_name = #{institutionName}
</select>
</mapper>